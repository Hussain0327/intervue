FROM node:20-slim

RUN useradd -m -s /bin/bash -u 1000 runner || true
WORKDIR /sandbox
RUN chown node:node /sandbox

COPY runner/run_node.sh /sandbox/run.sh
RUN chmod +x /sandbox/run.sh

USER node

ENTRYPOINT ["/sandbox/run.sh"]
