-- Enable pgvector extension for embeddings
CREATE EXTENSION IF NOT EXISTS vector;

-- Schema is managed by Alembic migrations.
-- Do NOT add table definitions here; they will conflict with migration history.
